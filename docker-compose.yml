version: "3.8"

services:
  web:
    build: ./web
    ports:
      - "3000:3000"
    environment:
      - DATABASE_URL=file:/data/local.db
    volumes:
      - ./web/local.db:/data/local.db

volumes: {}
